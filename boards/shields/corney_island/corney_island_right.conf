# Explicitly disable USB HID on the peripheral (right) side of the split keyboard.
# This silences Kconfig warnings caused by the controller board default (CONFIG_ZMK_USB=y)
# overriding the peripheral's unsatisfied dependency (!ZMK_SPLIT || ZMK_SPLIT_ROLE_CENTRAL).
CONFIG_ZMK_USB=n
